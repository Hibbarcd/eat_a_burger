<h1>Eat da Burger!</h1>

<div class="alignment">
<h2>Burgers available to eat!</h2>
<div class="left-uneaten">
<ul>
  {{#each burgers}}
    {{#unless hungry}}
      {{> burgers/burger-block hungry=true}}
    {{/unless}}
  {{/each}}
</ul>
</div>

<div class="right-eaten">
<h2>Need to be made again if you want to eat!</h2>
<ul>
  {{#each burgers}}
    {{#if hungry}}
      {{> burgers/burger-block hungry=false}}
    {{/if}}
  {{/each}}
</ul>
</div>
</div>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    <label for="slee">Consume?</label><br>
    <input type="radio" name="status" value="1" checked> Lets Eat!<br>
    <input type="radio" name="status" value="0"> Make another!
  </div>

  <button type="submit">Add Burger</button>
</form>
